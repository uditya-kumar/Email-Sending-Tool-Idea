# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Every env file except the committed template. The publishable key is designed
# to ship in the bundle, so this is hygiene rather than secrecy — but the rule
# should not depend on remembering which keys are safe.
.env
.env.*
!.env.example
